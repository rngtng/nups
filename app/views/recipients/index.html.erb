<h1>Listing recipients</h1>

<table>
  <tr>
    <th>Gender</th>
    <th>First name</th>
    <th>Last name</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @recipients.each do |recipient| %>
  <tr>
    <td><%= recipient.gender %></td>
    <td><%= recipient.first_name %></td>
    <td><%= recipient.last_name %></td>
    <td><%= recipient.email %></td>
    <td><%= link_to 'Show', [@account, recipient] %></td>
    <td><%= link_to 'Edit', edit_account_recipient_path(@account, recipient) %></td>
    <td><%= link_to 'Destroy', [@account, recipient], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Recipient', new_account_recipient_path(@account) %>
