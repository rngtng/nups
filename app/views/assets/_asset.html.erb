<% no_checkbox ||= false %>
<div id="file-<%= asset.id %>">
    <% unless no_checkbox %>
      <input type="checkbox" name="newsletter[attachment_ids][]" checked value="<%= asset.id %>">
    <% end %>
    <%= asset.name %> (<%= number_to_human_size asset.size %>) 
</div>