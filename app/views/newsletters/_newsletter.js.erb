updateNewsletter('<%= newsletter.id %>',
'<%= newsletter.state %>',
'<%= newsletter.progress_percent %>',
'<%= distance_of_time(newsletter.delivery_started_at || Time.now, newsletter.delivery_ended_at || Time.now, true) %>',
'<%= newsletter.sendings_per_second %>');