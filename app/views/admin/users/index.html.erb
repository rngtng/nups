<div id="users" class="index">
  <div  style="float:left">
    <h2>Users</h2>
  </div>
  <div  style="float:right">
    <%= link_to 'New User', new_admin_user_path %>
  </div>

  <table class="content">
    <tr>
      <th>Username</th>
      <th>Email</th>
      <th>Admin</th>
      <th></th>
    </tr>
  
    <% @users.each do |user| %>
      <tr class="<%= cycle('bgdark', 'bglight') %>">
        <td><%= user.username %></td>
        <td><%= user.email %></td>
        <td><%= user.admin %></td>
        <td><%= link_to 'Show', admin_user_path(user) %> &bull;
            <%= link_to 'Edit', edit_admin_user_path(user) %> &bull;
            <%= link_to 'Destroy', admin_user_path(user), :confirm => 'Are you sure?', :method => :delete %></td>
      </tr>
    <% end %>
  </table>
</div>